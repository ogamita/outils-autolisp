#!/bin/bash
# autolisp --- stub de dépréciation.
#
# L'ancien wrapper autolisp (AutoLISP-script) est déprécié et remplacé par
# `alfe` (AutoLISP front-end, projet clautolisp). L'ancien script reste
# disponible sous le nom `autolisp-deprecated` en cas de besoin ponctuel.
echo "autolisp est déprécié : utilisez plutôt 'alfe' (autolisp-front-end)." >&2
echo "(ancien script conservé sous autolisp-script/autolisp-deprecated)" >&2
exit 1
